# UI issues, 19 September 2026

Found by screenshotting every route at phone and desktop widths, then checked a second time against the screenshot.

## High

### content
- **/content/*** (390px, overflow). On a phone the left menu keeps its 210px width and the page gets a 160px column. Every Content screen becomes a strip of one-word lines. Session cards cut titles and rooms mid-word ("Early Morni", "Tata Innovatio") and lose the Edit link. Tables show only the first column, cut off, with no sign they scroll (speaker-manager, track-manager, exhibitor-manager, logistics-center, sponsor-tiering, advanced-banners, documents, session-qanda-manager). Affects all 44 Content routes. Fix: Under 768px hide .frame-left-side behind a menu button and let .frame-right-side take the full width. Then let session card headers wrap, and render tables as stacked cards or give them a visible horizontal scroll. `apps/organizer/src/app/globals.css (.frame-wrapper, .frame-left-side, .frame-right-side); only 2 media queries in the file`
- **/content/project-management/projects-and-checklists** (390px, broken). Task title, owner and due date are printed on top of each other ("Book" under "Tom due"). The owner and due columns are fixed at 130px and 110px, so this stays broken after the menu is fixed. Fix: Stack the task row vertically on narrow screens instead of a fixed flex row. `apps/organizer/src/app/(dash)/content/project-management/projects-and-checklists/page.tsx`

### tickets-a
- **/tickets/* (all 28 screens in this section)** (390px, overflow). The 200px side menu stays beside the content on a phone, so every page body is squeezed into about 160px and tables show one column at a time. Long page titles ("Confirmation", "MemberClicks", "YourMembership") and the tag and links beside them are cut off at the right edge. Fix: Add a max-width 700px rule that stacks the rail above the content, or collapses it behind a Menu button, and lets the content take full width. Let page titles break words. `apps/organizer/src/app/globals.css (.sidebar, only one phone media query in the file)`
- **/tickets/* (all screens)** (390px, overflow). The event header breaks on a phone: the date and venue wrap one word per line under the title and the Registrations and Agenda cards run off the right edge (div.cards ends at 488px). Fix: Stack the event meta and the stat cards in a column under 700px. `apps/organizer/src/app/(dash)/layout.tsx`
- **/tickets/ticket-setup/1-2-question-forms, /tickets/exhibitor-ticket-setup/2-2-question-forms, /tickets/exhibitor-ticket-setup/2-3-booth-selection, /tickets/exhibitor-ticket-setup/2-6-offline-payment, /tickets/exhibitor-ticket-setup/pre-paid-exhibitors** (390px, clipped). Form inputs, selects and help text are wider than the panel and are cut off at the right edge, so typed text and the booth help text cannot be read. Fix: Give form controls max-width: 100% and box-sizing: border-box, and let help text wrap. `apps/organizer/src/app/globals.css (.whova-form-group)`

### tickets-b
- **/tickets/payout** (390px, overflow). The left menu stays 200px wide beside the content, so the page content gets a 160px column. Affects every leaf page under /tickets/sponsor-ticket-setup, /tickets/payout, /tickets/publish-tickets, /tickets/ticket-marketing, /tickets/orders-and-transactions, /tickets/attendee-customization and the three guide pages. Fix: Below 700px, stack the menu above the content or hide it behind a button, and give the content the full width. `apps/organizer/src/app/globals.css`
- **/tickets/orders-and-transactions/transaction-history** (390px, clipped). Tables show only the first column. Amount, status and action links (Edit, Retire, Unsubscribe, Show) are off-screen inside a 110px scroll area, and rows grow to 400px tall. Affects transaction-history, attendee-orders, summary, publish-tickets, sponsor-tickets, registration-settings, registration-page, campaign-contact-list, campaign-link-tracking, referral-contest, social-sharing, event-listing, event-website, ticket-tiering. Fix: On phones render each row as a stacked card, or at least give the table the full screen width. `apps/organizer/src/app/(dash)/ui.tsx`
- **/tickets/sponsor-ticket-setup/sponsor-tickets** (390px, clipped). Form fields and their labels are wider than the panel and get cut off at the right edge, along with the help text. Affects sponsor-tickets (pass count), question-forms, email-campaign, campaign-contact-list, campaign-link-tracking, referral-contest, social-sharing. Fix: Set inputs, selects and textareas to width 100% with max-width, and let the panel wrap its text. `apps/organizer/src/app/(dash)/tickets/sponsor-ticket-setup/sponsor-tickets/pass-forms.tsx`

### attendees
- **/attendees/*** (390px, overflow). On a phone the 200px side menu stays beside the page, so the content column is 160px wide on all 28 Attendees routes. Tables show one column, stat tiles stack, paragraphs wrap to two or three words a line. Fix: Below 768px, move the side menu into a collapsible drawer and let .frame-right-side take the full width. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/layout.tsx`
- **/attendees/check-in-and-checkout/check-in** (390px, clipped). On a phone the badge code box, the Start camera button, the READY result panel and the Check in button on each row run off the right edge. Same on /attendees/check-in-and-checkout/kiosk-check-in. Staff will use this screen on a phone. Fix: Let .scan-left and the result panel stack at full width under 768px instead of a fixed 300px column. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/check-in-and-checkout/check-in/scanner.tsx`
- **/attendees/manage-attendees/attendees** (1280px, broken). Every attendee row shows the email as the name, a dash for title and company, "no ticket" and "opted out", while the summary above says 52 of 52 hold a ticket. The same bug makes /attendees/categories show everyone as Uncategorised, /attendees/segments show 0 for ticket and company groups, /attendees/ticket-session-mapping show Held by 0, Analytics & Exports show "No tickets issued yet", and leaves the Ticket type list empty in Add an attendee. Fix: Remove .select('email') from the queries in data.ts at lines 603, 607, 725 and 729 so the full documents are read. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/lib/data.ts`
- **/attendees/check-in-and-checkout/check-in** (1280px, excess-text). Developer notes and code names are shown to the organizer. Check-in: "Device dev_..., kept in this browser's localStorage. It is the checkInStations document id", "this is the row Whova puts an inline Check in button on", "Same idempotent write as a scan". Attendees: "Why this is two collections merged". Categories: "setCustomUserClaims". Admin Settings: "CONSOLE_ALLOWLIST", "src/lib/auth.ts". Segments: "RegistrationDoc.ticketType". Also Ticket Session Mapping, Session Cap, Release & Consent Forms and Kiosk Check-in. Fix: Replace each with one plain line, for example "This device is remembered as Console desk 1." Wrap the "Why" panels in GapPanel so SHOW_GAP_NOTES hides them. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/check-in-and-checkout/check-in/scanner.tsx`

### marketing
- **/marketing/*** (390px, overflow). On all 18 Marketing leaf screens the sidebar stays beside the content, so the page body is about 160px wide and tables show only their first column. Fix: Below 768px stack the sidebar above the content or hide it behind a menu button, and let tables scroll sideways. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/globals.css`
- **/marketing/*** (390px, clipped). On all 18 Marketing leaf screens the header tag is cut off ("1 thi", "22 pag") and the header button (View the live page, Create a tracked link, Open the site) is pushed off screen. Fix: Let the page header wrap so the tag and the button drop under the title. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`

### engagement
- **/engagement/announcements** (390px, overflow). On a phone the left menu stays beside the page, so the content column is about 160px wide on every Engagement screen (/engagement/announcements, /engagement/community/*, /engagement/gamification, /engagement/1-1-meeting-scheduler, /engagement/speed-networking, /engagement/round-table, /engagement/session-feedback, /engagement/surveys, /engagement/floormap, /engagement/announcement-wall/activity-stream-webpage, /engagement/live-polling, /engagement/photos/*). The announcement form wraps to three words a line and every table is cut to its first column. Fix: Below 768px stack the side menu above the content or collapse it behind a button so the content gets the full width. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/layout.tsx`
- **/engagement/announcements** (390px, clipped). Tables show only the first column on a phone. Announcements Sent shows Subject only, and the row actions are out of view on Session Feedback (Results, Edit, Unpublish), Round Table and 1-1 Meeting Scheduler (Edit, Cancel, Confirm, the Add button next to Name). Also affects Attendee Matchmaking, Floormap, Photo Collection, Activity Stream Webpage. Fix: Render table rows as stacked cards under 768px, with the actions on their own line. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`

### virtual-and-hybrid
- **/virtual-and-hybrid/attendee-activity** (390px, overflow). The sidebar menu stays beside the content on a phone, leaving the page about 160px wide, so every title, tile, table and form is squeezed into a narrow strip (all 15 /virtual-and-hybrid pages). Fix: Below 768px stack the sidebar under the content or collapse it into a drawer so the content column gets the full width. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/globals.css`
- **/virtual-and-hybrid/virtual-and-hybrid-setup** (390px, overflow). In the event header the Registrations and Agenda cards sit on top of the date and venue text, which wraps one word per line, and the Agenda card runs off the right edge to 488px (all 15 /virtual-and-hybrid pages). Fix: Stack .buttons-cards vertically on phones and let .cards wrap inside the viewport. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/layout.tsx`
- **/virtual-and-hybrid/attendee-activity** (390px, clipped). Tables show only their first column on a phone with no way to scroll sideways. Attendee Activity loses Ticket, App, Sessions, Hours and Wrote. Rehearsal Sessions shows only Day and the page is 12,349px tall. Event Checklist loses the progress bars. Setup loses Delivered by. Fix: Give tables a visible horizontal scroll at full content width or switch rows to stacked cards under 600px. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/virtual-and-hybrid/online-session-manager/rehearsal-sessions** (390px, overflow). Header actions and links fall outside the white page card: Message Speakers and the orange tag on Rehearsal Sessions, Open Projects & Checklists on Event Checklist, and the third link on Setup, Attendee Activity and Attendance Gamification. Fix: Let the PageHeader title row wrap and place actions and links on their own lines under the title on narrow screens. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`

### tools-pay-publish
- **/tools/*, /pay/*** (390px, overflow). On a phone the 200px sidebar stays beside the page, so every screen in Tools and Pay gets a 168px content column with text wrapping one or two words per line (all 16 leaf pages). Fix: Add a max-width 768px rule that stacks .frame-wrapper into one column and collapses the sidebar behind a menu button. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/globals.css`
- **/tools/*, /pay/*, /publish** (390px, overflow). The event masthead breaks on a phone: the date and venue wrap one word per line down the left edge and the Registrations and Agenda cards run off the right side to 488px. Fix: Below 768px stack .buttons-cards vertically and let .cards wrap to full width. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/layout.tsx`
- **/tools/app-adoption/app-download-button** (390px, excess-text). The "QR as SVG" block prints the whole SVG path as wrapped text, making the page 15,466px tall on a phone (also on /tools/app-adoption/downloadable-graphics, where it runs off the right edge at 1280). Fix: Replace the raw SVG text with a Download SVG button and a Copy button, or cap the block at a few lines with overflow hidden. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/app-adoption/snippet.tsx`
- **/tools/app-adoption/web-app-link** (390px, clipped). Tables show only their first column on a phone with the rest cut off and no scroll cue (also /tools/moderator-tools/session-chats, /pay/balance, /pay/billing-information, /pay/publish, /pay/order-details). On /publish this hides the Fix links, so the checklist cannot be acted on. Fix: Below 768px render table rows as stacked cards, or wrap tables in a visible horizontal scroller. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/tools/app-adoption/downloadable-graphics** (390px, clipped). The desk sign preview is wider than the column, so only a slice of "Get the conference app" shows and the QR is off screen; the page title is also cut at "Downloadab". Fix: Scale the sign preview to the container width with max-width 100% and let the h1 wrap. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/app-adoption/downloadable-graphics/page.tsx`
- **/tools/moderator-tools/community-board** (390px, clipped). Post titles are cut off ("Tram f Manha around 08:00?") and reply text wraps one word per line, with the Hide link squeezed against the author name. Fix: Let the post header wrap onto two lines and move Hide under the text on narrow screens. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/moderator-tools/community-board/page.tsx`
- **/tools/moderator-tools/community-board** (1280px, other). The blue note says "Hide takes a post out of the app immediately", and replies get the same Hide link, but the app still shows hidden replies. Fix: Filter replies on status == visible in useReplies in the app, the same way posts are filtered. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/lib/data/community.ts`

### web
- **/tickets/invoice** (390px, overflow). The form and the "How it works" column stay side by side on a phone. The right column is about 70px wide with one word per line, and the page scrolls sideways to 487px (472px at 375). At 768 the same column is squeezed to about 260px. Fix: Stack the two columns under 900px: replace the inline gridTemplateColumns "minmax(0,420px) minmax(0,1fr)" with a class that goes single column. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/tickets/invoice/page.tsx`
- **/tickets/checkout** (390px, excess-text). The Register card opens with a red engineering note, "Ticket sales are not configured on this deployment. STRIPE_SECRET_KEY is not set...", and ends with "No ticket can be bought until a payment processor is configured." Same on /tickets/exhibitor and /tickets/sponsor. /tickets/invoice shows "Test mode. No payment processor is configured on this deployment...". Fix: Show buyers one plain line such as "Ticket sales open soon" and keep the env var detail in server logs. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/tickets/checkout-form.tsx`
- **/hcls** (390px, overflow). The H1 "The Healthcare and Life Sciences Symposium (HCLS)" is set so large that "Symposium" runs past the screen edge, and the hero image, date line and buttons follow it out to 407px (same at 375). Fix: Cap the H1 with clamp() at about 34px on phones, allow overflow-wrap, and give the hero column min-width: 0. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/hcls/page.tsx`

### app
- **/community/[id]** (390px, clipped). From code, confirmed in the live bundle: on the web build the tab bar is fixed to the bottom of the window and nothing reserves space for it. The Reply box and Send button on a community topic sit under the tabs. The same strip hides the last row on every tab screen (Home, Agenda, People, Community, Me, Surveys, Documents, Logistics, survey Submit). No screenshot: the tool cannot sign in to the app. Fix: Add bottom padding equal to the tab bar height on web to the tab content container, or to Screen and the reply bar. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/(tabs)/_layout.tsx:53 and app/(tabs)/community/[id].tsx:248`

## Medium

### content
- **/content/speaker-center/message-speakers** (390px, overflow). In the send box "Type N to confirm" overlaps itself and the passphrase input runs past the card. Same on message-sponsors and message-exhibitors. Fix: Give the confirm inputs width:100% and put the label on one line. `apps/organizer/src/app/(dash)/messaging/message-form.tsx`
- **/content/artifact-center-poster-pitch-gallery/artifact-manager** (390px, broken). In the empty-state box the action link prints over the last line of the message ("event." under "Poster sessions are on the agenda"). Same on both outreach-campaigns pages, video-hosting and fair-manager. Fix: Let the empty-state box grow with its content instead of a fixed height. `apps/organizer/src/app/(dash)/ui.tsx (EmptyState)`
- **/content/basics** (1280px, excess-text). Engineering notes are shown as page copy: "PROJECT kgc-conference-app-and-website (LIVE)", "Stamped on every top-level document and leading every composite index", "A compile-time constant in @kgc/shared" (logistics-center), "fixed in models.ts" (sponsor-tiering), "SessionDoc has no sponsor field" (sponsor-tiering), "the confirmation email in @kgc/scripts has its own hard-coded address" (app-branding). Fix: Delete code names and file paths from visible copy and keep one plain sentence per field. `apps/organizer/src/app/(dash)/content/basics/page.tsx, apps/organizer/src/app/(dash)/content/logistics-center/page.tsx, apps/organizer/src/app/(dash)/content/sponsor-center/sponsor-tiering/page.tsx, apps/organizer/src/app/(dash)/content/branding-center/app-branding/page.tsx`
- **/content/agenda-center/track-manager** (1280px, excess-text). Long paragraphs explain design choices where the organizer needs one instruction: "There is no delete. Sessions carry track ids, and Firestore has no cascade..." (track-manager, speaker-manager, logistics-center), the Agenda cache check paragraphs, "A second per-sponsor weight would mean two answers..." (advanced-banners), "What is stored is a typed name against the hash of the wording..." (release-and-consent-forms), "These are distinct strings, not people..." (message-team-members). Fix: Cut each to one line, for example "Tracks cannot be deleted. Remove the track from its sessions instead." `apps/organizer/src/app/(dash)/content/agenda-center/track-manager/page.tsx`
- **/content/exhibitor-center/exhibitor-manager** (1280px, ai-voice). Warnings are written as one-liners instead of facts: "It is otherwise an argument at the desk with somebody who is already holding a box of leaflets", "Reserving the word costs nothing and settles the argument before it reaches a flyer" (branded-event-url), "never a comma, which is half the world's way of writing a name" (session-manager), "publishing an agenda to a thousand phones is not undone by editing" (session-manager), "Sending twice is indistinguishable from a mistake in somebody's inbox" (message screens). Fix: State the fact and the action only, for example "1 exhibitor is over their pass allocation." `apps/organizer/src/app/(dash)/content/exhibitor-center/exhibitor-manager/page.tsx`

### tickets-a
- **/tickets/ticket-setup/1-3-confirmation-emails, /tickets/ticket-setup/member-and-invite-only-ticketing, /tickets/ticket-setup/session-rsvp, /tickets/ticket-setup/1-5-registration-widgets** (390px, hierarchy). Table rows grow 200 to 470px tall because long-text columns wrap off screen. The confirmation email page is about 6,500px of mostly blank rows. Caused by the side menu squeeze. Fix: On phones render each row as a stacked card, or cap the long column with a line clamp. `apps/organizer/src/app/(dash)/ui.tsx (Table)`
- **/tickets/exhibitor-ticket-setup/2-5-ticket-add-ons** (1280px, empty-state). The empty Extras table shows a full sentence in 20px bold grey inside a 400px tall box. The Netlify badge covers part of it. On a phone the text overlaps the table header. Fix: Pass a short string such as "No extras yet" as the empty prop and move the instruction to a normal paragraph under the table. `apps/organizer/src/app/(dash)/tickets/exhibitor-ticket-setup/2-5-ticket-add-ons/page.tsx line 177; globals.css .whova-empty-table`
- **/tickets/ticket-setup/discount-codes** (1280px, empty-state). The page says "No discount codes has been entered" with no form and no reason. The cause, a missing Stripe key, shows only in a small grey tag. Fix: Replace the empty state with one line: "Discount codes need a Stripe key." `apps/organizer/src/app/(dash)/tickets/ticket-setup/discount-codes/page.tsx`
- **/tickets/ticket-setup/1-1-create-tickets, /tickets/ticket-setup/create-group-tickets, /tickets/ticket-setup/1-6-abandoned-registration, /tickets/ticket-setup/1-7-registration-settings, /tickets/exhibitor-ticket-setup/registration-settings, /tickets/exhibitor-ticket-setup/2-2-question-forms, /tickets/exhibitor-ticket-setup/pre-paid-exhibitors** (1280px, excess-text). Engineering notes are shown to the organizer: "apps/web, port 3200", "Seats come from items on the order document", "The timestamp comes from refundedAt", "quantitySold >= quantityTotal ... npm run reconcile:sold", "orders reference a ticket type by id", "The two collections have no link". Fix: Delete these notes or cut each to one plain line, for example "Tickets cannot be deleted. Hide them instead." `apps/organizer/src/app/(dash)/tickets/ticket-setup/1-7-registration-settings/page.tsx; create-group-tickets/page.tsx; 1-6-abandoned-registration/page.tsx`
- **/tickets/ticket-setup/1-2-question-forms, /tickets/exhibitor-ticket-setup/2-2-question-forms, /tickets/exhibitor-ticket-setup/2-6-offline-payment, /tickets/exhibitor-ticket-setup/pre-paid-exhibitors, /tickets/exhibitor-ticket-setup/2-5-ticket-add-ons, /tickets/ticket-setup/1-6-abandoned-registration** (1280px, excess-text). Long opinion copy sits above and inside simple forms: "A hundred distinct sentences is a list, not a distribution", "They have done nothing wrong", "this is the record of what your word was based on", "worth reading this table in April rather than in May", "The one recoverable case". Fix: Keep one short hint per field at most, for example "Required. Saved on the order." and drop the rest. `apps/organizer/src/app/(dash)/tickets/question-form-editor.tsx; manual-order-form.tsx; exhibitor-ticket-setup/2-5-ticket-add-ons/page.tsx`
- **/tickets/ticket-setup/ticket-add-ons, /tickets/ticket-setup/member-and-invite-only-ticketing, /tickets/ticket-setup/session-rsvp, /tickets/ticket-setup/memberclicks-connection-guide, /tickets/ticket-setup/imis-connection-guide, /tickets/ticket-setup/yourmembership-connection-guide, /tickets/ticket-setup/neon-crm-connection-guide** (1280px, excess-text). These screens are essays about what is missing: "There are exactly two, and they are booleans on the ticket type", "The three ways to restrict a ticket, ranked by what we have", "Missing. There is no per-session registration document", field names like SessionDoc.capacity. Fix: Reduce each to a two-line statement of what the organizer can do and a link to the screen where they do it, or hide the nav item. `apps/organizer/src/app/(dash)/tickets/ticket-setup/session-rsvp/page.tsx; ticket-add-ons/page.tsx; member-and-invite-only-ticketing/page.tsx`
- **/tickets/ticket-setup/1-1-create-tickets, /tickets/exhibitor-ticket-setup/2-3-booth-selection, /tickets/ticket-setup/1-2-question-forms** (390px, tap-target). Row actions (Edit, Hide, Release, Block, Move up, Remove) are 12px text links with no padding, stacked a few pixels apart, and sit in the last table column which is off screen on a phone. Fix: Give row actions at least 32px height and spacing, and keep them visible in a stacked phone row. `apps/organizer/src/app/(dash)/tickets/ticket-setup/1-1-create-tickets/page.tsx lines 262-286`

### tickets-b
- **/tickets/orders-and-transactions/attendee-orders** (390px, overflow). The pager text "Items 1-2 of 2" is drawn on top of the left menu. Same on campaign-contact-list and transaction-history. Fix: Let the pager wrap inside the panel instead of overflowing to the left. `apps/organizer/src/app/(dash)/ui.tsx`
- **/tickets/sponsor-ticket-setup/sponsor-tickets** (390px, empty-state). The empty state under Passes to be named is cut off on both sides. The heading reads "Not inputte" and the button overflows the box. Same on question-forms, confirmation-emails, summary, exhibitor-orders, sponsor-orders. Fix: Let the empty state shrink to the panel width and drop the fixed table header above it when there are no rows. `apps/organizer/src/app/(dash)/ui.tsx`
- **/tickets/orders-and-transactions/transaction-history** (1280px, excess-text). Failed emails print the raw provider response in the table: '403 {"statusCode":403,"name":"validation_error","message":"You can only send testing emails..."}'. Fix: Show a short reason such as "Sending domain not verified" and keep the raw response behind a details toggle. `apps/organizer/src/app/(dash)/tickets/orders-and-transactions/transaction-history/page.tsx`
- **/tickets/ticket-marketing/email-campaign** (1280px, spacing). Subject is a 150px unstyled box that cuts off its own placeholder, and the message box is 180px wide on a 1000px panel. The confirm label reads "Type3to confirm" with no spaces. Fix: Use the standard form input style at full width, and add spaces around the count in the label. `apps/organizer/src/app/(dash)/tickets/ticket-marketing/email-campaign/campaign-form.tsx`
- **/tickets/sponsor-ticket-setup/sponsor-tickets** (1280px, excess-text). Engineering notes and long essays are shown as help text. Sponsor Tickets: "A number, not a bullet... reading the digit out of the copy would break the first time somebody wrote four". Question Forms: a 60-word intro above Add a question. Also registration-settings ("quantitySold >= quantityTotal"), attendee-categories ("UserDoc.roles... firestore.rules"), publish-tickets ("Collapsing the two into one ready light"), link forms ("exactly what a phishing campaign wants"), email-campaign ("A count is the thing you cannot check", shown twice), payout "Why these numbers do not match", event-listing, social-sharing "Before you post". Fix: Cut each to one plain line or remove it. Example: "Passes included per package sold." `apps/organizer/src/app/(dash)/tickets/sponsor-ticket-setup/sponsor-tickets/page.tsx`
- **/tickets/export-to-ams-crm** (1280px, other). The Export to AMS/CRM page is titled for AMS/CRM but talks about Zapier and says "Import it into Zapier with their own import tool", which makes no sense. The MemberClicks page title says "export guide" while the menu says "connection guide". Fix: Write this page for a generic CRM export and match the page title to the menu label. `apps/organizer/src/app/(dash)/tickets/export-to-ams-crm/page.tsx`

### attendees
- **/attendees/call-for-volunteers/volunteer-manager** (390px, clipped). On a phone, form inputs and their hints are wider than the panel and are cut off on the right. Same on /attendees/certificates and /attendees/check-in-and-checkout/session-self-check-in. Fix: Give .whova-form-group and .whova-text-input max-width: 100% and drop the fixed 200px width on small screens. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/form.tsx`
- **/attendees/release-and-consent-forms** (390px, broken). On a phone the empty state text is drawn on top of the page title and the panel below it, so both are unreadable. Same on /attendees/call-for-volunteers/release-and-consent-forms and /attendees/manage-attendees/cross-event-report. Fix: Remove the fixed width and negative offsets on .whova-empty-state so it sizes to its panel. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/attendees/name-badges** (390px, overflow). Badge previews are 3.5in wide and push the page to 941px. Names and QR codes are cut off. The Print button sits off screen in the header. Fix: Wrap .badge-sheet in a horizontally scrolling box on screen, keep the fixed size for print only, and move Print below the title on small screens. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/name-badges/page.tsx`
- **/attendees/*** (390px, clipped). Header tags, action buttons and the third header link run off the right edge ("52 to print", "+ New form", "Export Check-in Lists", "Orders summary"). Seen on most leaf pages. Fix: Let the PageHeader title row wrap, and put tags and actions on their own line under 768px. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/attendees/manage-attendees/attendee-limit-upgrade** (1280px, excess-text). Pages are engineering notes with no controls: "What hybrid would actually require", "A player in Expo Go", "WP-06", "no stream field exists". Same on /attendees/manage-attendees/hybrid-settings, /attendees/check-in-and-checkout/self-check-in and /attendees/check-in-and-checkout/checkout. Fix: Cut each page to one sentence of status and a link to the screen that does the job, or hide these routes from the nav. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/manage-attendees/hybrid-settings/page.tsx`
- **/attendees/check-in-and-checkout/check-in** (390px, tap-target). Row Check in buttons are 28px tall, filter chips 20px, sort links 16px, pager arrows 28px and the row menu 28px. Too small to hit on a phone at a busy desk. Chips and sort links repeat on Attendees, Categories, Segments, Session Cap and Name Badges. Fix: Set a 40px minimum height on .btn-sm, .whova-tag-main links, .pager-btn and .row-actions-btn under 768px. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/check-in-and-checkout/check-in/desk-table.tsx`

### marketing
- **/marketing/*** (1280px, hierarchy). The header action (Open the site, Create a tracked link, View /about) shows as plain dark text with no button or underline. It does not look clickable. Fix: Style the header action as a button, like Step-by-step setup guide. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/marketing/organizer-co-promo/page.tsx`
- **/marketing/event-webpages/logistics-webpage** (1280px, hierarchy). The Venue tile prints the full address at stat-number size and wraps to four lines, making the tile row twice as tall. Also on /marketing/event-webpages/venue-map-webpage. Fix: Show the address at body size or move it out of the stat tiles. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/marketing/event-webpages/logistics-webpage/page.tsx`
- **/marketing/social-media-center/content-library** (1280px, excess-text). Engineering notes shown to the organizer: "Nothing in this repo composes an image from a template", "The ticket-receipt work put a real email sender in the project; this uses it." Fix: Replace with "None yet" and "Sends from the dashboard". `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/marketing/social-media-center/content-library/page.tsx`
- **/marketing/whova-listing/my-event-listing** (1280px, excess-text). Tables name code paths to the organizer: "apps/web layout. Needs a deploy", "Rendered from ticketTypes on every request". /marketing/event-website says "code. Needs a deploy" on nine rows and adds a footnote about Firestore and React files. Fix: Say "Ask the developer" or "Edited in Create Tickets" and drop the footnotes. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/marketing/whova-listing/my-event-listing/page.tsx`
- **/marketing/social-wall/activity-stream-webpage** (1280px, ai-voice). Essay-style copy and headings: "Gated by the registered claim, and posted on that understanding. Publishing it retroactively changes the deal." Same tone in headings elsewhere: "Would a visitor notice anything missing?", "Where the material actually is", "What a wall would be showing the lobby", "stated so a number here can be argued with". Fix: Use plain labels: "Not public", "Missing details", "Assets", "Posts by category". `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/marketing/social-wall/activity-stream-webpage/page.tsx`
- **/marketing/*** (1280px, other). The "Powered by Netlify" badge sits on top of the last table column and hides text ("static page. Needs a d..."). Seen on Logistics Webpage, Content Library and Special-Purpose. Fix: Turn off the Netlify badge in site settings.

### engagement
- **/engagement/session-feedback** (390px, overflow). At 390px the page header does not wrap. The status tag and the action sit outside the white card on the grey background: '+ New survey' on Session Feedback and Surveys, '+ New poll' on Live Polling, 'Open the wall' on Activity Stream Webpage, and the link row on 1-1 Meeting Scheduler and Speed Networking. Fix: Let PageHeader wrap: put tags, links and actions on their own rows under the title at phone width. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/engagement/community/discussion-topics** (390px, overflow). Community post rows do not wrap. The title is squeezed to one word a line and the author and date spill outside the card. Affects Meet-ups, Discussion Topics, Social Groups. Fix: Add flex-wrap to the post header row and move author and date under the title on narrow screens. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/community/category-screen.tsx`
- **/engagement/session-feedback** (1280px, hierarchy). The main action looks like plain text. '+ New survey', '+ New poll', 'Create the first one', 'Build the schedule' and the round table 'Add' use whova-btn-main without 'primary' and render grey or borderless, while Send on Announcements is blue. At 390px 'Create the first one' wraps to four lines of plain black text. Fix: Add the primary class to the one main action on each screen. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/survey-screen.tsx`
- **/engagement/round-table** (1280px, other). Form fields are unstyled browser defaults: small boxes and native selects on Round Table, 1-1 Meeting Scheduler, Speed Networking and the survey form. They do not match the Announcements form. Fix: Add the whova-text-input class (and a styled select) to these inputs. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/gathering-forms.tsx`
- **/engagement/announcements** (1280px, other). Time sent shows a raw timestamp: '2026-08-27T22:03:06.235Z'. Fix: Format it as a local date and time, for example 'Aug 27, 6:03 PM'. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/announcements/page.tsx`
- **/engagement/session-feedback** (1280px, excess-text). The info tip on Session Feedback and Surveys says 'the app has no screen that renders a survey yet, so a published one is not yet answerable from a phone'. That is wrong now: the app has a survey screen and this survey has 12 responses. Fix: Delete that sentence from both pages. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/session-feedback/page.tsx`
- **/engagement/announcements** (1280px, excess-text). Helper copy argues instead of instructing. Announcements: 'Deliberately the only option. A queued blast fires whether or not anybody is awake to stop it...' and 'Counted live from the attendee list, so this is the number it will actually reach.' Speed Networking: 'Whoever turns up: that is genuinely the input. Duplicates are dropped case-insensitively...'. Round Table: 'Eight to ten is the number a conversation survives. Past that it becomes a panel with no microphone.' Matchmaking: 'Said plainly because a silently truncated list reads as a complete one.' Fix: Cut each hint to one plain line or remove it, for example 'Seats at the table.' and 'One name per line.' `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/announcements/announcement-form.tsx`
- **/engagement/gamification** (1280px, excess-text). Pages for unbuilt features show engineering notes to the organizer. Gamification: 'Of the four things a conference normally awards points for, one ... could be scored honestly today' and an info tip about Firestore triggers and IAM grants. Profile Photo Frames: 'the wider denominator would blame the wrong thing'. Photo Booth tip: 'React Native has no canvas'. Floormap tip: 'no document in the model has a field to hang a floorplan on'. Live Polling tip names the tallyPoll trigger. Photo Collection shows field names like users.photoURL and a warning paragraph. Fix: Replace with one line saying the feature is not available yet, and move the notes behind SHOW_GAP_NOTES. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/gamification/page.tsx`
- **/engagement/round-table** (1280px, other). The 'Powered by Netlify' badge floats over page content. At 1280px it covers the Cancel link on the second Round Table row. At 390px it covers the warning banner on Announcements and stat tiles elsewhere. Fix: Turn the badge off in the Netlify site settings.

### virtual-and-hybrid
- **/virtual-and-hybrid/adv-stream-integration/zoom** (1280px, hierarchy). Buttons render as plain black text with no border or fill, so they do not look clickable: the empty-state actions on Streaming Setup, Zoom, Microsoft Teams, Attendance Gamification, Other Tools and Tutorials and Tips, plus Message Speakers and Open Projects & Checklists. Fix: Add the primary or secondary modifier to these whova-btn-main links, or give the base class a default fill. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/globals.css`
- **/virtual-and-hybrid/attendance-gamification** (390px, overflow). On a phone the empty-state box is wider than its card and the action text spills out of the bottom of the box (Attendance Gamification, Zoom, Microsoft Teams, Streaming Setup, Other Tools, Tutorials and Tips). Fix: Set max-width 100% and height auto on .whova-empty-state and let the action wrap inside it. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/virtual-and-hybrid/online-session-manager/streaming-setup** (1280px, empty-state). Seven of the eleven screens in this tab are dead ends that say "Not inputted yet" with a button to an unrelated page, such as "Attendance is at the door" on Zoom and "Go to Report" on Tutorials and Tips. Fix: Hide the unbuilt screens from the nav, or replace each empty state with one line saying the feature is not available. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/online-session-manager/streaming-setup/page.tsx`
- **/virtual-and-hybrid/online-session-manager/streaming-setup** (1280px, other). Empty states read wrong: "Not inputted yet" and "No streams has been entered for this event", "No Zoom meetings has been entered", "No tutorials has been entered". Fix: Change the NotInputted copy to "Nothing here yet" and "No {what} yet." so plurals work. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/virtual-and-hybrid/logistics-management/emergency-manager** (1280px, excess-text). Engineering notes are shown to the organizer. Setup prints "includesVideoLibrary entitlement is false" in code font under the table. The Emergency Manager banner ends "an announcement, which is real". The info tips mention "fcmTokens", "a development build rather than Expo Go" and the "tasks collection". Fix: Cut these to one plain sentence each, for example "This page stores the plan. It does not alert anyone." `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/logistics-management/emergency-manager/page.tsx`

### tools-pay-publish
- **/pay/publish** (1280px, other). The sales tax page is named "Publish" in the Pay menu and in its heading, the same name as the top-level Publish tab. Fix: Rename the nav title and h1 to "Sales Tax". `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/lib/nav.ts`
- **/tools/admin-control/code-access-control** (1280px, excess-text). Engineering notes are shown to the organizer: "firestore.rules starts allowing reads" and "Step 3 is a manual run of scripts/set-claims.ts today". Fix: Delete the "How somebody actually gets in" panel or cut it to one plain sentence without file names. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/admin-control/code-access-control/page.tsx`
- **/tools/report** (1280px, excess-text). The report shows developer text: "PROJECT kgc-conference-app-and-website (LIVE)", "The Admin SDK bypasses firestore.rules entirely, which is the correct posture..." and "An in-process ring buffer: it empties on restart...". Audit times are raw ISO strings in UTC. Fix: Remove the three notes and format times as local date and time. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/report/page.tsx`
- **/pay/publish** (1280px, excess-text). Long tax explainer plus a table quoting code ("txcd_20030000", "automatic_tax: { enabled: true }", "which is why turning it on early was safe") (similar on /pay/billing-information: "See SETUP-PAYMENTS.md §5"). Fix: Cut to a three-row checklist with Done or To do and one Stripe link per row. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/pay/publish/page.tsx`
- **/tools/moderator-tools/session-chats** (1280px, excess-text). An empty feature page explains internals: "Thread membership comes from participantIds and the rules deny everyone else... a much larger decision than a screen" (the Photos page is likewise an empty shell). Fix: Hide Photos and Session Chats from the menu until the app has those features, or reduce each to one line. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/moderator-tools/session-chats/page.tsx`
- **/tools/app-adoption/app-adoption-email** (1280px, ai-voice). Helper copy reads as written by a model: "That is the cost this email exists to reduce", "the conference app is where your ticket actually lives", "The install sentence comes from one constant shared with the public site..." (also "a wrong number that looks entirely reasonable" on /pay/publish). Fix: Delete the helper notes and rewrite the email body in plain sentences. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/tools/app-adoption/app-adoption-email/page.tsx`

### web
- **/** (390px, other). On first visit the cookie notice covers the lower third of the phone screen on every page, hiding "See the agenda" in the home hero and the first ticket card on /tickets, and the "Powered by Netlify" badge sits on top of the notice. Fix: Make the notice a single slim bar at the bottom edge with the button inline, and turn off the Netlify badge in site settings. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/components/cookie-consent.tsx`
- **/learn** (390px, clipped). The hero tagline "Learn how knowledge graphs can shape the future of your business and career." and the blue strapline sit on top of the dark wave mesh and cannot be read. Same at 768. Fix: Move the text below the artwork on small screens or put a solid panel behind it. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/learn/page.tsx`
- **/** (390px, spacing). Sponsor logos stack one per row in 210px tiles, so the sponsor wall takes about 4,500px of scrolling on the home page and again on /sponsor. Fix: Use a two column grid with shorter tiles under 600px. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/components/sponsor-tiers.tsx`
- **/tickets** (390px, hierarchy). Workshops and Virtual are bare text rows under two full cards, with faint stray "·" marks hanging at line ends. The two cheaper tickets look like footnotes. Fix: Give Workshops and Virtual the same card as Main Conference, and hide the dot separators when features wrap to separate lines. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/tickets/tier-card.tsx`
- **/documents** (390px, broken). All three documents link to "example.invalid", which is printed under each title, so nothing opens. Fix: Replace the three placeholder documents in the dashboard with real files, or hide the list until they exist. Drop the hostname line. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/documents/page.tsx`
- **/startup-pitch** (390px, excess-text). Important dates reads "The 2027 calendar is not confirmed yet, so this page states no dates. The application deadline and the date of the pitch event appear here once they are set." /call-for-posters has the same paragraph. Fix: Replace with "Dates to be announced." and the contact address. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/startup-pitch/page.tsx`
- **/tickets/exhibitor** (390px, spacing). The hero buttons "Register" and "Attendee tickets" touch with no gap, and the date and venue line is printed twice in a row. Same on /tickets/sponsor. Fix: Add a 12px gap to the button row and remove the repeated "3-7 May 2027 at Cornell Tech..." sentence opening. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/tickets/audience-page.tsx`
- **/** (390px, other). The FAQ expand marker renders as a 7px dot with a speck beside it, so questions do not look tappable. Affects /, /tickets and every page with the FAQ block. Fix: Use a chevron or plus sign at 12px or larger. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/globals.css`

### app
- **/home** (390px, spacing). From code, confirmed in the live bundle: every signed-in header on web adds 68px of top padding for a tab pill that used to float at the top. The tabs now sit at the bottom, so each screen opens with an empty band above the title. Affects all tab roots and pushed screens. No screenshot: the tool cannot sign in to the app. Fix: Set WEB_TAB_BAR to 0 for the top inset and move that allowance to the bottom. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/components/pushed-header.tsx:25, components/whova-header.tsx:149, components/screen-header.tsx:34`
- **/login** (390px, other). A 'Powered by Netlify' badge floats over the bottom right corner of the app. After sign-in that corner holds the Me tab and the Send buttons. Fix: Turn off the Netlify badge for the kgc27-app site in Netlify site settings.
- **/people** (390px, tap-target). From code: small controls rely on hitSlop to reach 44pt, and hitSlop does nothing in the web build, so on a phone browser the A to Z rail letters are 16 by 13px, Follow is 28px tall, filter chips 34px and the Messages icon 30px. Affects /people, /agenda, /community, /home. Fix: Give these controls real padding or minHeight of 44 on web instead of hitSlop. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/(tabs)/people/index.tsx:864, components/follow-button.tsx:62, components/filter-chip.tsx:69, components/messages-button.tsx:42`
- **/login** (390px, tap-target). From code: 'Back', 'Send another code' and 'Use a different address' are bare 20px text links with no padding, and the last two are stacked 8px apart. Fix: Give each link minHeight 44 and centre the text. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/login.tsx:301-323, 460-473, 537-550`
- **/login** (390px, ai-voice). Em dashes in attendee copy, all present in the live bundle: the code step on /login ('Codes expire after 10 minutes ... send another'), the length hint on /change-password, and two lines on /me/badge ('already checked in ... tell them', 'sort it out ... bring the claim code'). Fix: Split each into two sentences and drop the dash. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/login.tsx:245, app/change-password.tsx:264, app/(tabs)/me/badge.tsx:155, app/(tabs)/me/badge.tsx:241`
- **/me/badge** (390px, excess-text). From code: under the QR code sits a caption of two to three paragraphs, about 60 to 80 words, in small grey text covering offline use, the boarding pass warning and brightness. Fix: Keep one line ('Works offline. Do not share a photo of this code.') and drop the rest. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/(tabs)/me/badge.tsx:148-160`

## Low

### content
- **/content/agenda-center/session-manager** (1280px, ai-voice). Em dashes in visible copy. Session Manager import help ("reported rather than invented", "separated by a semicolon"), Track Manager import help ("does not rename anything"), Speaker Manager import help ("as bios and headshots arrive"). Fix: Split each into two sentences. `apps/organizer/src/app/(dash)/content/agenda-center/session-manager/page.tsx, apps/organizer/src/app/(dash)/content/agenda-center/track-manager/page.tsx, apps/organizer/src/app/(dash)/content/speaker-center/speaker-manager/page.tsx`
- **/content/exhibitor-center/passport-contest** (1280px, empty-state). Empty states read "Not inputted yet" and "No prospects has been entered for this event", which is ungrammatical for every plural noun. Seen on 12 Content screens. Fix: Change the template to "No {what} yet." `apps/organizer/src/app/(dash)/ui.tsx:385`
- **/content/exhibitor-center/exhibitor-manager** (1280px, excess-text). The notes panel says the app and website have no exhibitor page. Both now have one, so the note is wrong. Fix: Remove the note. `apps/organizer/src/app/(dash)/content/exhibitor-center/exhibitor-manager/page.tsx:282`
- **/content/project-management/projects-and-checklists** (1280px, hierarchy). The only way to mark a task done is to click its small status pill, which looks like a label, not a button. Fix: Add a checkbox or a "Mark done" button next to Edit. `apps/organizer/src/app/(dash)/content/project-management/projects-and-checklists/page.tsx:54`
- **/content/agenda-center/session-manager** (1280px, other). Each session card shows a drag handle, but sessions cannot be dragged. Fix: Remove the handle until drag to reschedule exists. `apps/organizer/src/app/(dash)/content/agenda-center/session-manager/page.tsx:267`
- **/content/basics** (1280px, hierarchy). A greyed-out Save button sits in the header of a page with nothing to save. Fix: Remove the button and label the page read-only. `apps/organizer/src/app/(dash)/content/basics/page.tsx:65`
- **/content** (390px, tap-target). The expand arrows in the left menu are 23x11px, too small to hit with a thumb. On every Content page. Fix: Make the whole menu row the toggle, at least 40px tall. `apps/organizer/src/app/(dash)/menu.tsx`

### tickets-a
- **/tickets/ticket-setup/create-group-tickets, /tickets/ticket-setup/discount-codes, /tickets/ticket-setup/1-6-abandoned-registration, /tickets/exhibitor-ticket-setup/2-2-question-forms, /tickets/exhibitor-ticket-setup/2-4-confirmation-emails, /tickets/exhibitor-ticket-setup/2-6-offline-payment** (1280px, other). Empty states read "No group orders has been entered", "No questions has been entered". The verb is wrong for plurals. Fix: Change the NotInputted copy to "No {what} yet." `apps/organizer/src/app/(dash)/ui.tsx line 385`
- **/tickets/ticket-setup/1-3-confirmation-emails** (1280px, ai-voice). Email subjects use an em dash between "Your KGC 2027 ticket" and the ticket name, and a seeded question hint has one after "Not a booking". The banner promises "the send is re-run" but there is no re-run control. Raw provider JSON is printed in each failed row. Fix: Use a colon in the subject and hint, drop the re-run promise or add the button, and show only the provider message text. `apps/organizer/src/app/(dash)/tickets/ticket-setup/1-3-confirmation-emails/page.tsx; scripts/src/lib (email templates)`
- **/tickets/* (all screens)** (1280px, other). A "Powered by Netlify" badge floats over the right side of the content and covers table text. Fix: Turn off the Netlify badge in the site settings.

### tickets-b
- **/tickets/sponsor-ticket-setup/question-forms** (390px, other). Empty states read "No questions has been entered for this event", "No orders has been entered", "No campaigns has been entered". Seen on question-forms, sponsor-tickets, confirmation-emails, summary, exhibitor-orders, sponsor-orders, email-campaign. Fix: Change the NotInputted copy to "No questions yet" so it works for plurals. `apps/organizer/src/app/(dash)/ui.tsx`
- **/tickets/ticket-marketing/event-listing** (1280px, ai-voice). Copy uses the "X rather than Y" and aphorism pattern throughout: "it is a relationship rather than a feature", "Codes are free; ambiguity is not", "spends your best send on your worst impression", "a list, not a distribution". Seen on event-listing, social-sharing, event-website, question-forms. Fix: Rewrite as plain statements or remove. `apps/organizer/src/app/(dash)/tickets/ticket-marketing/event-listing/page.tsx`
- **/tickets/payout** (1280px, other). Banner text runs two words together: "what was soldrather than what has landed". Fix: Add the missing space after the italic word. `apps/organizer/src/app/(dash)/tickets/payout/page.tsx`
- **/tickets/sponsor-ticket-setup/registration-settings** (1280px, hierarchy). Empty Opens and Closes cells show two long dashes run together, with no gap between the columns. Fix: Show "Always" or leave the cell blank, and add cell padding. `apps/organizer/src/app/(dash)/tickets/audience-registration.tsx`
- **/tickets/ticket-marketing/referral-contest** (1280px, ai-voice). Seeded link labels use long dashes, for example "Speaker referral" then a dash then "Ada Lovelace". Also on campaign-link-tracking and campaign-contact-list. Fix: Replace the dashes in the demo data with a colon or comma.

### attendees
- **/attendees/call-for-volunteers/volunteer-manager** (1280px, ai-voice). Field hints try to be clever: "The only number that matters at 07:05 on day one.", "Who you will be looking for at the door.", "Local wall clock." Empty states read "Not inputted yet" and "No volunteers has been entered". Fix: Drop the hints or use plain ones ("Optional"). Change the empty state to "No volunteers yet." `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/call-for-volunteers/volunteer-manager/form.tsx`
- **/attendees/integrations/mailchimp** (1280px, excess-text). Integration pages comment on the build and on Whova: "exactly what tickets/ticket-marketing/email-campaign is unbuilt for want of", "this is the highest-leverage integration on the list", "Whova's sync runs every 24 hours, which is not meaningfully fresher". The Zapier step says "Import it into Zapier with their own import tool", which is not how Zapier works. Same on /attendees/integrations/constant-contact and /attendees/integrations/crm-integration-via-zapier. Fix: Keep the three export steps, delete the commentary, and rewrite the Zapier steps or remove the page. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/integrations/mailchimp/page.tsx`
- **/attendees/session-cap** (1280px, clipped). Narrow column headers break mid-word ("Roo m seat s", "Coun ted in", and "Direc tory" on the Attendees table). Room names are cut to "Tata Innovation Center Audi..." and the Room cell sits higher than the rest of the row. Fix: Widen those columns or shorten the labels ("Seats", "Counted"), let room names wrap, and align the cell to the middle. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/session-cap/page.tsx`
- **/attendees/check-in-and-checkout/session-self-check-in** (390px, other). The Sessions table lists all 86 sessions with no paging. The page is about 6,500px tall on desktop and 15,000px on a phone. Fix: Use the same Pagination and day filter as Session Cap. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/check-in-and-checkout/session-self-check-in/page.tsx`
- **/attendees/manage-attendees/attendees** (1280px, tiny-text). The "yes", "not yet", "fits" and "opted out" tags are 10px and stat tile labels are 11px. Fix: Raise small tags and tile labels to 12px. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/attendees/release-and-consent-forms** (1280px, hierarchy). "+ New form" renders as plain grey text in the header corner, so the only action on the page does not look clickable. Fix: Style it as the primary button. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/release-and-consent-forms/page.tsx`
- **/attendees/check-in-and-checkout/check-in** (1280px, ai-voice). Em dashes in visible text: the list is named "KGC 2027 (em dash) Main Door" in the header, chip and banner (also on Kiosk Check-in), and empty table cells use a long dash across Attendees, Categories, Segments and Check-in. Fix: Rename the list "KGC 2027 Main Door" and use a blank or "-" for empty cells. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/attendees/check-in-and-checkout/check-in/page.tsx`

### marketing
- **/marketing/*** (390px, other). Each screen puts its limits behind a small "i" next to the title. On a phone it is a tiny tap target. Fix: Open the tip on tap and keep it inside the viewport, or show one line under the title. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/marketing/whova-listing/my-event-listing** (1280px, empty-state). Whova Listing, Social Wall Customization and Venue Map Webpage are menu items for things that do not exist (tags read "not applicable", "no public wall", "0 of 8 pinned"). Fix: Hide these three from the nav until they do something, or merge them into Event Website. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/lib/nav.ts`
- **/marketing/organizer-co-promo** (1280px, ai-voice). Link labels in live data carry em dashes ("Speaker referral, Ada Lovelace"). Also shown on /marketing/whova-listing/traffic-analytics. Fix: Rename the links with a colon or comma in Campaign Link Tracking. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/marketing/whova-listing/traffic-analytics/page.tsx`

### engagement
- **/engagement/community/meet-ups** (1280px, ai-voice). Stat tile subtitles read as commentary: 'counted, not RSVPs', 'counted at read time', 'counted, not read off a tally' (Live Polling), 'breaks when they move' (Photo Collection). Fix: Use plain labels or drop the subtitle: 'replies', 'reactions', 'votes'. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/community/category-screen.tsx`
- **/engagement/gamification** (1280px, empty-state). Empty states say 'Not inputted yet' and 'No polls has been entered for this event', which is odd wording and wrong grammar. Header tags also read 'not inputted yet' on Photo Collection, Photo Booth and Profile Photo Frames. Fix: Say 'No polls yet', 'No surveys yet', and drop the tag on pages that have nothing to input. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/engagement/session-feedback** (1280px, clipped). The 'Questions' column header breaks mid word as 'Questio / ns'. Fix: Widen the column or add white-space: nowrap to table headers. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/engagement/survey-screen.tsx`

### virtual-and-hybrid
- **/virtual-and-hybrid/logistics-management/emergency-manager** (390px, excess-text). Five of the seven emergency form fields carry a helper paragraph, such as "This is the single most useful line on the card" and "One named person who decides. Not a team address." On a phone the form runs over 1,400px and placeholders are cut off inside the inputs. Fix: Drop the helper text except for the procedure field and shorten placeholders. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/logistics-management/emergency-form.tsx`
- **/virtual-and-hybrid/virtual-and-hybrid-setup** (1280px, ai-voice). Setup copy is odd: tile "Money taken for them", heading "What the buyer was told", and a Delivered by column that says "nothing" on every row. The source also has a "Sold, not delivered" tag and a "refund conversation waiting to happen" banner that show once a remote ticket sells. Event Checklist tip reads "A report, not a second editor". Fix: Use plain labels: "Virtual tickets sold", "Revenue", and one sentence saying streaming is not set up. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/virtual-and-hybrid-setup/page.tsx`
- **/virtual-and-hybrid/attendee-activity** (1280px, ai-voice). Empty table cells show an em dash, 81 on the first page of Attendee Activity, and Event Checklist uses one in the Done tile. Fix: Leave empty cells blank or show 0. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/attendee-activity/page.tsx`
- **/virtual-and-hybrid/attendee-activity** (1280px, other). The Wrote column shows codes like "1p 1r" with no legend, and tile labels are vague: "On the roll", "Nothing yet", "the follow-up list". Fix: Show "1 post, 1 reply" and rename the tiles Attendees, Active and Inactive. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/attendee-activity/page.tsx`
- **/virtual-and-hybrid/online-session-manager/rehearsal-sessions** (1280px, other). Rehearsal Sessions lists all 83 talks in one unpaged table with raw dates like 2027-05-03, and the second heading reads "The ones with nowhere to rehearse". Fix: Group or filter by day, format dates as Mon May 3, and rename the heading "No free slot". `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/virtual-and-hybrid/online-session-manager/rehearsal-sessions/page.tsx`

### tools-pay-publish
- **/tools/moderator-tools/photos** (1280px, empty-state). Empty states read "Not inputted yet. No attendee photos has been entered for this event" (also "No scans has been entered", "No orders has been entered" on /tools/report, and on session-chats and moderate-session-qanda). Fix: Change the NotInputted copy to "No {what} yet." `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/tools/moderator-tools/photos** (390px, spacing). The empty-state button text "Moderate the community board" spills out of the grey box and overlaps its border (same on /tools/moderator-tools/session-chats). Fix: Let the NotInputted box grow with its content and give the action link normal padding. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/(dash)/ui.tsx`
- **/publish** (1280px, other). The checklist says "No order has ever been fulfilled" and Balance shows 0 orders while Order Details lists 2 paid test purchases, with nothing explaining that test orders are excluded. Fix: Add "excluding test orders" to the tile subtext and the checklist detail. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/lib/commerce.ts`
- **/tools/moderator-tools/community-board** (1280px, other). The "Powered by Netlify" badge floats over the right column and covers a Hide link. Fix: Remove the Netlify badge snippet from the dashboard site.
- **/tools/moderator-tools/community-board** (1280px, ai-voice). Seeded demo posts use em dashes, for example after "Happy to coordinate" in the tram post. 7 on the page. Fix: Replace the em dashes in the seed data with commas or full stops.
- **/tools/*, /pay/*** (390px, tap-target). Sidebar links are 25 to 28px tall and the expand arrows are 23x11px. Fix: Raise sidebar rows to 40px and give the arrow a 32px hit area on touch screens. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/organizer/src/app/globals.css`

### web
- **/agenda** (390px, other). Sessions titled "A media archive" list the speaker as "(Phil) (Meredith)" with a blank avatar. It also shows in the home page schedule. Fix: Fix the speaker names on that session in the dashboard or hide speakers with no real name. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/components/event-schedule.tsx`
- **/agenda** (390px, spacing). Track and room metadata wraps mid line and leaves a dangling "·" at the end of rows; "All days" is a 15,800px scroll with no way to jump between days once past the chips. Fix: Put room and type on their own line, and make the day chips sticky. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/agenda/page.tsx`
- **/exhibitors** (390px, clipped). The logo placeholder tile cuts the name to "Graphwis" and "Ontotext Labs" at 11.9px text, and there is a 130px empty gap between aisles. Fix: Show initials in the placeholder tile and reduce the section gap. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/exhibitors/page.tsx`
- **/previous-events** (390px, spacing). The footer is about 1,300px tall on a phone, longer than the content on /previous-events, /announcements, /documents and /kgc-lifetime-achievement-awards. Fix: Lay the three footer link groups out in two columns and shrink the large KGC wordmark on phones. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/components/site-footer.tsx`
- **/** (390px, other). The home schedule day tabs cut off after Wed with a sliver of the next tab and no sign that the row scrolls. Fix: Shrink the tabs so all five days fit in 342px, or add an edge fade. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/components/event-schedule.tsx`
- **/team** (390px, tap-target). LinkedIn icons beside each person are 20px square. Same on /learn. Checkout radio inputs are 13px, though the whole row is tappable. Fix: Pad the icon links to 40px. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/components/linkedin-icon.tsx`
- **/blog** (390px, tap-target). Category filter chips are 28px tall and wrap into five tight rows. Fix: Raise chip height to 36px with 8px row gap. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/blog/page.tsx`
- **/documents** (390px, excess-text). Helper copy states the obvious: "Hosted elsewhere. Each opens in a new tab." On /previous-events: "These open the archived sites, which are maintained separately from this one." On /blog: "Each post is summarised here and published in full on knowledgegraph.tech." Fix: Delete these lines. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/documents/page.tsx`
- **/tickets/sponsor** (390px, ai-voice). Hero and feature copy reads as written for effect: "rather than a few thousand collecting tote bags", "Sessions, not just signage", "The attendee list, properly", "which is how most sponsor budgets are actually spent." Fix: Rewrite as plain statements of what the sponsor gets. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/tickets/audience-page.tsx`
- **/announcements** (390px, ai-voice). The live announcement "Moved to Bloomberg 165..." uses an em dash. It also runs in the home ticker. Fix: Edit the announcement text in the dashboard.
- **/tickets/options** (390px, other). An internal design review page, "Ten ticketing pages ... the only thing that differs is the part being judged", is publicly reachable on the live site. It renders without breakage, as does /tickets1. Fix: Remove the route or block it from production before launch. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/apps/web/src/app/tickets/options/page.tsx`

### app
- **/login** (390px, excess-text). A failed sign-in with no network shows 'Cannot reach the server. Is the emulator running?', which is a developer note. /change-password tells the attendee the length rule is 'Firebase's minimum, not ours'. Both strings are in the live bundle. Fix: Use 'Cannot reach the server. Check your connection.' and 'At least 6 characters.' `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/login.tsx:184, app/change-password.tsx:264`
- **/home** (390px, excess-text). Load errors explain internals to attendees, for example 'Access is carried in the token this device received when you signed in' and 'this build of the app is missing a database index for it'. Shown only when a screen fails to load. Fix: Cut each to one line: what failed and what to do (try again, sign in again, or see the registration desk). `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/components/data-error.tsx:85-143`
- **/login** (390px, excess-text). The landing says how to sign in twice: 'Your ticket gets you in. Choose how you want to sign in.' above the buttons and 'First time here? Choose Create account and we will email you a code.' below them. Fix: Drop the line under Welcome and keep the first time hint. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/login.tsx:336-338, 390-392`
- **/login** (390px, other). The KGC mark shows as a pale square that does not match the grey page behind it. Fix: Export kgc-mark.png with a transparent background. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/login.tsx:215`
- **/login** (390px, other). The browser tab has no title, so the page shows as the bare URL in tabs, history and share sheets. Fix: Set a web title such as 'KGC 2027' in app.json or the root layout. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/app.json`
- **/community** (390px, clipped). From code: the New topic sheet has no keyboard handling and the body box grows without a scroll container, so a long post runs under the keyboard. Fix: Wrap the sheet in KeyboardAvoidingView and cap the body box with maxHeight. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/(tabs)/community/index.tsx:624-686`
- **/home/survey/[id]** (390px, clipped). From code: the survey screen has text answers but no KeyboardAvoidingView, so a free text question near the bottom can sit behind the keyboard on iOS. Session Q&A on /agenda/[id] has the same gap. Fix: Wrap both screens in KeyboardAvoidingView with behavior 'padding' on iOS. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/(tabs)/home/survey/[id].tsx:319, components/session-qa.tsx:82`
- **/+not-found** (390px, ai-voice). From code: the not found screen says 'The route you followed isn't part of the app.' Fix: Use 'Page not found.' with a button to Home. `/Users/hartigan/Documents/Claude/Projects/KGC/2KGC-App/app/src/app/+not-found.tsx:15`
